#include <iostream>

using namespace std;

int main() {
    // Logical operations:
    // Or -> ||
    // And -> &&
    // Not -> !

    if(true || false) {
        cout << "The expression above is true" << endl;
    }
}
